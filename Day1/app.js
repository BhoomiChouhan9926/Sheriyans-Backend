const express = require('express')
 const app = express() // this will create the server ans the instance of the server will be stored in the app
app.listen(3000); // it will satrt the server at the port 3000
